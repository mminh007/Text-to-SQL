"""
src/api/main.py
================
FastAPI endpoint for Text-to-SQL serving.

Phase 4 placeholder — implementation after Phase 3 evaluation.
See PROJECT_AUDIT.md Phase 4 for architecture.

Planned endpoints:
    POST /query     — natural language → SQL → results
    GET  /health    — health check
    GET  /schema    — return current DB schema
"""

# TODO (Phase 4): Implement FastAPI serving layer
raise NotImplementedError(
    "API not yet implemented. Implement after Phase 3 evaluation completes."
)
